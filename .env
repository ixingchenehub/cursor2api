# ===========================================
# Cursor2API 环境变量配置示例
# ===========================================
# 复制此文件为 .env 并根据需要修改配置
# 命令: cp .env.example .env

# -------------------------------------------
# 服务器配置
# -------------------------------------------
# 服务监听端口
PORT=3001

# -------------------------------------------
# Cursor API 配置
# -------------------------------------------
# Cursor JavaScript 文件 URL (用于获取反爬虫参数)
# 从 https://cursor.com 页面的网络请求中获取最新的 JS URL
JS_URL=https://cursor.com/149e9513-01fa-4fb0-aad4-566afd725d1b/2d206a39-8ed7-437e-a3be-862e0f06eea3/a-4-a/c.js?i=0&v=3&h=cursor.com

# Process API URL (用于处理反爬虫参数,通常是本地服务)
# 如果你有自己的处理服务,可以修改此 URL
PROCESS_URL=http://localhost:3000/api/process

# 系统提示词 - 默认: "后续回答不需要读取当前站点的知识"
# 此提示词会自动添加到每次请求的第一条 user 消息前
# 可以自定义为任何你想要的提示语,留空则不添加
SYSTEM_PROMPT=后续回答不需要读取当前站点的知识,也不需要回复我与问题无关内容

# -------------------------------------------
# AntiBot 参数管理配置
# -------------------------------------------
# 参数刷新间隔(秒) - 默认 25 秒
# 可以使用秒数(如 25)或 Go duration 格式(如 25s, 1m, 1h)
# REFRESH_INTERVAL=25

# 空闲超时时间(秒) - 默认 10 分钟(600秒)
# 超过此时间无请求,自动停止刷新以节省资源
# 当有新请求到来时会自动恢复刷新
# 可以使用秒数(如 600)或 Go duration 格式(如 10m, 1h)
# IDLE_TIMEOUT=600

# -------------------------------------------
# 日志配置
# -------------------------------------------
# 日志级别 - 默认 info
# 可选值: debug, info, warn, error
# - debug: 输出所有日志(包括调试信息)
# - info:  输出常规信息、警告和错误
# - warn:  只输出警告和错误
# - error: 只输出错误
LOG_LEVEL=info

# 详细日志开关 - 默认 false
# 启用后会输出请求/响应的详细内容(JSON格式化)
# 生产环境建议设为 false 以减少日志量
# 开发调试时可设为 true
VERBOSE_LOGGING=false

# -------------------------------------------
# 其他配置 (可选)
# -------------------------------------------
# 请求超时时间(秒)
# REQUEST_TIMEOUT=30

# 最大重试次数
# MAX_RETRIES=3
